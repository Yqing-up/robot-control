请在终端中依次执行以下命令来完成git合并：

1. 添加已解决冲突的文件：
   git add src/views/ManagementView.vue

2. 检查状态：
   git status

3. 完成合并提交：
   git commit -m "解决合并冲突：合并chatApi和头部控制API导入"

4. 推送到远程仓库（可选）：
   git push origin main
